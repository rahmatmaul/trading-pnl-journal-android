package com.tradingpnl.journal

/**
 * Versioned launcher component used so Android launchers refresh the adaptive
 * icon after an in-place update while the app package and database stay intact.
 */
class LauncherV3Activity : MainActivity()
